/**
 * 验证作者信息更新
 */

const db = require('../models/database');

console.log('════════════════════════════════════════════════════════');
console.log('           论文作者信息验证');
console.log('════════════════════════════════════════════════════════\n');

const paper = db.db.prepare('SELECT * FROM papers').get();

console.log('📄 论文信息:\n');
console.log(`标题：${paper.title}\n`);

console.log('✏️  作者信息:');
console.log(`${paper.authors}\n`);

// 解析作者列表
const authorList = paper.authors.split(',').map(a => a.trim());
console.log('作者列表:');
authorList.forEach((author, idx) => {
  console.log(`   ${idx + 1}. ${author}`);
});

console.log('\n📅 提交日期:', paper.submitted_date);
console.log('🏷️  会议标记:', paper.comments?.match(/accepted at (.+?)(?:\.|$)/i)?.[1] || '无');

console.log('\n════════════════════════════════════════════════════════');
console.log('                    ✅ 验证完成');
console.log('════════════════════════════════════════════════════════\n');
